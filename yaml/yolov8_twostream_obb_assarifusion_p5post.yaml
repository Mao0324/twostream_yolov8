# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLOv8 two-stream OBB with ASSARIFusion at intermediate stages and one post-P5 ASSARIFusion before normal ADD.

nc: 5
scales:
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]
scale: s

backbone:
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 3, C2f, [128, True]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8

  - [-4, 1, Conv, [64, 3, 2]] # 4
  - [-1, 1, Conv, [128, 3, 2]] # 5
  - [-1, 3, C2f_Faster, [128, True]] # 6
  - [-1, 1, Conv, [256, 3, 2]] # 7

  - [-3, 1, ASSARIFusion, [64, 8, 2, True]] # 8

  - [-4, 6, C2f, [256, True]] # 9
  - [-1, 1, Conv, [512, 3, 2]] # 10
  - [-4, 6, C2f_Faster, [256, True]] # 11
  - [-1, 1, Conv, [512, 3, 2]] # 12

  - [-3, 1, ASSARIFusion, [128, 8, 4, True]] # 13

  - [-4, 6, C2f, [512, True]] # 14
  - [-1, 1, Conv, [1024, 3, 2]] # 15
  - [-4, 6, C2f_Faster, [512, True]] # 16
  - [-1, 1, Conv, [1024, 3, 2]] # 17

  - [-3, 1, ASSARIFusion, [256, 8, 4, True]] # 18

  - [-4, 3, C2f, [1024, True]] # 19
  - [-1, 1, SPPF, [1024, 5]] # 20
  - [-4, 3, C2f_Faster, [1024, True]] # 21
  - [-1, 1, SPPF, [1024, 5]] # 22

  - [-3, 1, ASSARIFusion, [512, 8, 4, True]] # 23 post-P5 RGB/IR refinement
  - [-4, 1, Silence, []] # 24 refreshed RGB P5 after ASSARIFusion
  - [-4, 1, Silence, []] # 25 refreshed IR P5 after ASSARIFusion

  - [[9, 11], 1, ADD, [1]] # 26 P3 normal ADD
  - [[14, 16], 1, ADD, [1]] # 27 P4 normal ADD
  - [[24, 25], 1, ADD, [1]] # 28 P5 normal ADD after post-P5 ASSARIFusion

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 29
  - [[-1, 27], 1, Concat, [1]] # 30
  - [-1, 3, C2f, [512]] # 31
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 32
  - [[-1, 26], 1, Concat, [1]] # 33
  - [-1, 3, C2f, [256]] # 34
  - [-1, 1, Conv, [256, 3, 2]] # 35
  - [[-1, 31], 1, Concat, [1]] # 36
  - [-1, 3, C2f, [512]] # 37
  - [-1, 1, Conv, [512, 3, 2]] # 38
  - [[-1, 28], 1, Concat, [1]] # 39
  - [-1, 3, C2f, [1024]] # 40
  - [[34, 37, 40], 1, OBB, [nc, 1]]
